import { testRender } from '../../../test_utils'

import Input from './input'
import VisibilityIcon from './visibility_icon'

it('Input Should Render', () => testRender(Input))
it('VisibilityIcon Should Render', () => testRender(VisibilityIcon))
